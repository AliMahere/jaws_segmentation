# Generated convenience mirror of pyproject.toml's [project.dependencies].
# pyproject.toml is the source of truth; regenerate this after changing deps there.
#
# GPU (CUDA 12.4) wheels, matching the versions this repo was verified against:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
# CPU-only wheels (used by the Dockerfile/CI):
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0
numpy==2.4.6
matplotlib==3.11.1
tqdm==4.70.0
progressbar2==4.6.0
